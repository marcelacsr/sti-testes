<%= form_for(current_user.received_notifications
                 .build, remote: true) do |f| %>
    <div><%= hidden_field_tag :follower_id, @user.id %></div>
    <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
